## v1.0.2 - Auth Module Patch Release
Routine auth module release keeps version metadata aligned with the repository. No functionality changes were introduced in this patch.

### Changed
- Bumped the published `@instago/auth` version to v1.0.2 so the release metadata matches the latest commit.

## v1.0.1 - Auth Module Maintenance Release
Maintenance release for the auth module with updated release metadata and changelog; no functional changes.

### Changed
- Updated release metadata and created the changelog entry.
